<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <li><%=link_to "<i class='fa fa-circle-o-notch'></i> SENAI ".html_safe, root_path, class:"navbar-brand" %></li>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user %>
        <ul class="nav navbar-nav">
          <li><%=link_to " <i class='fa fa-plus-square' aria-hidden='true'></i> New Task".html_safe, new_task_path, class:"navbar-brand" %></li>
        </ul>
      <% end %>
        
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><%=link_to '<i class="fa fa-pencil" aria-hidden="true"></i> Edit profile'.html_safe, edit_user_registration_path %></li>
          <li><%=link_to '<i class="fa fa-sign-out" aria-hidden="true"></i> Sign out'.html_safe, destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%=link_to '<i class="fa fa-comment-o" aria-hidden="true"></i> About'.html_safe, about_path %></li>
          <li><%=link_to '<i class="fa fa-sign-in" aria-hidden="true"></i> Sign in'.html_safe, new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>